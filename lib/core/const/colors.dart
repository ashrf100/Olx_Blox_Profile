import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primaryDark = Color(0xFF002f34);
  static const Color primary = Color.fromARGB(255, 19, 121, 133);
  static const Color primaryLight = Color(0xFF23E5DB);
  static const Color primaryVariant = Color(0xFFFfffff);

  // Error Colors
  static const Color error = Color.fromARGB(255, 202, 2, 39);
  static const Color errorDark = Color(0xFFCF6679);
}
